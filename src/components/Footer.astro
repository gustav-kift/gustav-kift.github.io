---
import { siteConfig } from "../config";

const hasProjects = siteConfig.projects?.length > 0;
const hasExperience = siteConfig.experience?.length > 0;
const hasEducation = siteConfig.education?.length > 0;
---

<footer class="relative bg-[#0c131f] border-t border-gray-800 py-12">
  <div class="mx-auto max-w-7xl px-6">

    <!-- Top Section -->
    <div class="flex flex-col md:flex-row justify-between gap-12">

      <!-- Left Column -->
      <div class="flex flex-col gap-4 max-w-sm">
        <h3 class="text-2xl font-bold text-gray-100">
          {siteConfig.name}
        </h3>

        <p class="text-gray-400 text-base leading-relaxed">
          {siteConfig.title}
        </p>

        <!-- Social Icons -->
        <div class="flex gap-6 mt-2">
          <a
            href={`mailto:${siteConfig.social.email}`}
            aria-label="Email"
            class="text-gray-300 hover:text-[var(--accent-color)] transition-colors"
            style={`--accent-color: ${siteConfig.accentColor}`}
          >
            <!-- Email -->
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
              fill="none" stroke="currentColor" stroke-width="2"
              stroke-linecap="round" stroke-linejoin="round">
              <path d="M3 7a2 2 0 0 1 2 -2h14a2 2 0 0 1 2 2v10a2 2 0 0 1 -2 2h-14a2 2 0 0 1 -2 -2v-10z"></path>
              <path d="M3 7l9 6l9 -6"></path>
            </svg>
          </a>

          <a
            href={siteConfig.social.linkedin}
            target="_blank"
            class="text-gray-300 hover:text-[var(--accent-color)] transition-colors"
            style={`--accent-color: ${siteConfig.accentColor}`}
            aria-label="LinkedIn"
          >
            <!-- LinkedIn -->
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
              fill="none" stroke="currentColor" stroke-width="2"
              stroke-linecap="round" stroke-linejoin="round">
              <path d="M8 11v5"></path>
              <path d="M8 8v.01"></path>
              <path d="M12 16v-5"></path>
              <path d="M16 16v-3a2 2 0 1 0 -4 0"></path>
              <path d="M3 7a4 4 0 0 1 4 -4h10a4 4 0 0 1 4 4v10a4 4 0 0 1 -4 4h-10a4 4 0 0 1 -4 -4z"></path>
            </svg>
          </a>

          <a
            href={siteConfig.social.twitter}
            target="_blank"
            class="text-gray-300 hover:text-[var(--accent-color)] transition-colors"
            style={`--accent-color: ${siteConfig.accentColor}`}
            aria-label="Twitter"
          >
            <!-- Twitter -->
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
              fill="none" stroke="currentColor" stroke-width="2"
              stroke-linecap="round" stroke-linejoin="round">
              <path d="M4 4l11.733 16h4.267l-11.733 -16z"></path>
              <path d="M4 20l6.768 -6.768m2.46 -2.46l6.772 -6.772"></path>
            </svg>
          </a>

          <a
            href={siteConfig.social.github}
            target="_blank"
            class="text-gray-300 hover:text-[var(--accent-color)] transition-colors"
            style={`--accent-color: ${siteConfig.accentColor}`}
            aria-label="GitHub"
          >
            <!-- GitHub -->
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24"
              fill="none" stroke="currentColor" stroke-width="2"
              stroke-linecap="round" stroke-linejoin="round">
              <path d="M9 19c-4.3 1.4 -4.3 -2.5 -6 -3m12 5v-3.5c0 -1 .1 -1.4 -.5 -2
              c2.8 -.3 5.5 -1.4 5.5 -6a4.6 4.6 0 0 0 -1.3 -3.2a4.2 4.2 0 0 0 -.1 -3.2
              s-1.1 -.3 -3.5 1.3a12.3 12.3 0 0 0 -6.2 0c-2.4 -1.6 -3.5 -1.3 -3.5 -1.3
              a4.2 4.2 0 0 0 -.1 3.2a4.6 4.6 0 0 0 -1.3 3.2c0 4.6 2.7 5.7 5.5 6
              c-.6 .6 -.6 1.2 -.5 2v3.5"></path>
            </svg>
          </a>
        </div>
      </div>

      <!-- Right Column -->
      <div class="flex flex-col items-start md:items-end gap-6">

        <!-- Site Navigation -->
        <nav class="flex flex-wrap gap-6 text-gray-400">
          <a href="#about" class="hover:text-gray-200 transition-colors">About</a>

          {hasProjects && (
            <a href="#projects" class="hover:text-gray-200 transition-colors">Projects</a>
          )}

          {hasExperience && (
            <a href="#experience" class="hover:text-gray-200 transition-colors">Experience</a>
          )}

          {hasEducation && (
            <a href="#education" class="hover:text-gray-200 transition-colors">Education</a>
          )}
        </nav>

        <!-- Download Button -->
        <a
          href="/resume.pdf"
          download
          class="inline-block bg-white/10 hover:bg-white/20 text-white px-4 py-2 rounded-md border border-gray-700 transition-colors"
        >
          Download Resume
        </a>

        <!-- Copyright -->
        <p class="text-gray-500 text-sm">
          © {new Date().getFullYear()} {siteConfig.name}. All rights reserved.
        </p>
      </div>

    </div>
  </div>

  <!-- Background Pattern -->
  <div class="absolute inset-0 pointer-events-none -z-10 overflow-hidden opacity-40">
    <svg class="absolute bottom-0 left-0 w-full h-24 text-gray-800">
      <pattern id="footer-pattern" width="100" height="100" patternUnits="userSpaceOnUse">
        <path d="M0 50 Q 25 40, 50 50 T 100 50"
          stroke="currentColor"
          stroke-width="0.4"
          fill="none">
        </path>
      </pattern>
      <rect width="100%" height="100%" fill="url(#footer-pattern)"></rect>
    </svg>
  </div>
</footer>
